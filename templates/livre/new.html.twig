{% extends 'base.html.twig' %}

{% block title %}New Livre{% endblock %}

{% block body %}
    <div class="row">
        <div class="col-8 offset-2 mt-5 mb-5 p-4 login">
            <h1 class="mb-3">Create new Livre</h1>

            {{ include('livre/_form.html.twig') }}

            <a href="{{ path('livre_index') }}" class="btn btn-primary mt-3 mb-3 btn-sm">
                <i class="fas fa-arrow-alt-circle-left"></i> back to list
            </a>
        </div>
    </div>

{% endblock %}

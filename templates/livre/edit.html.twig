{% extends 'base.html.twig' %}

{% block title %}Edit Livre{% endblock %}

{% block body %}
    <div class="row">
        <div class="col-8 offset-2 login p-4 mt-5 mb-5">
            <h1 class="mt-3 mb-4">Edit Livre</h1>

            {{ include('livre/_form.html.twig', {'button_label': 'Update'}) }}

            {# class="btn btn-primary mt-3 mb-3" <i class="fas fa-arrow-alt-circle-left"></i> #}
            <a href="{{ path('livre_index') }}" class="btn btn-primary mt-3 mb-3 btn-sm"> <i class="fas fa-arrow-alt-circle-left"></i> back to list</a>

            {{ include('livre/_delete_form.html.twig') }}
        </div>
    </div>


{% endblock %}

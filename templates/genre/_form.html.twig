{{ form_start(form) }}
    {{ form_widget(form) }}
    <button class=" btn btn-dark text-light mt-3 mb-3 btn-sm"> <i class="fas fa-save"></i> {{ button_label|default('Save') }}</button>
{# class="btn btn-primary mt-3 mb-3" <i class="fas fa-arrow-alt-circle-left"></i> #}

{{ form_end(form) }}

{% extends 'base.html.twig' %}

{% block title %}Edit Auteur{% endblock %}

{% block body %}
    <div class="row">
        <div class="col-8 offset-2 login mt-4 mb-4 p-4">
            <h1 class="mt-2 mb-4">Edit Auteur</h1>

            {{ include('auteur/_form.html.twig', {'button_label': 'Update'}) }}

            <a href="{{ path('auteur_index') }}" class="btn btn-primary mt-3 mb-3 btn-sm"><i class="fas fa-arrow-alt-circle-left"></i> back to list</a>


            {{ include('auteur/_delete_form.html.twig') }}
        </div>
    </div>


{% endblock %}

{% extends 'base.html.twig' %}

{% block title %}New Genre{% endblock %}

{% block body %}

    <div class="row">
        <div class="col-8 offset-2 login p-4 mt-4 mb-4">
            <h1 class="mt-2 mb-4">Create new Genre</h1>

            {{ include('genre/_form.html.twig') }}

            <a href="{{ path('genre_index') }}" class="btn btn-primary mt-3 mb-3 btn-sm"> <i class="fas fa-arrow-alt-circle-left"></i> back to list</a>

        </div>
    </div>


{% endblock %}

{% extends 'base.html.twig' %}

{% block title %}Edit Genre{% endblock %}

{% block body %}
    <div class="row">
        <div class="col-8 offset-2 login p-4 mt-4 mb-4">
            <h1 class="mt-3 mb-4">Edit Genre</h1>

            {{ include('genre/_form.html.twig', {'button_label': 'Update'}) }}

            <a href="{{ path('genre_index') }}" class="btn btn-primary mt-1 mb-1 btn-sm"><i class="fas fa-arrow-alt-circle-left"></i> back to list</a>


            {{ include('genre/_delete_form.html.twig') }}
        </div>
    </div>

{% endblock %}
